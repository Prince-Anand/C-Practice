#include <stdio.h>

int main()
{
    printf("Hello-");
    printf("World"); // Hello-World on same line as compiler does not print to new line by itself
    printf("\n");    // Output Jumps on next line
    printf("This is on new line.");
    return 0;
}